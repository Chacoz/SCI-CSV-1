{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && if [ -n \"$DATABASE_URL\" ]; then ( export DATABASE_URL=\"${DATABASE_URL_UNPOOLED:-${POSTGRES_URL_NON_POOLING:-$DATABASE_URL}}\"; prisma db push --skip-generate && node prisma/seed.mjs ); fi && next build"
}
