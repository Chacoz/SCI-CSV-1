{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && if [ -n \"$DATABASE_URL\" ]; then prisma db push --skip-generate && node prisma/seed.mjs; fi && next build"
}
